source "https://rubygems.org"

# Mirrors the gem set GitHub Pages builds with, so `bundle exec jekyll serve`
# locally matches what ships on github.io.
gem "github-pages", group: :jekyll_plugins
gem "webrick"
